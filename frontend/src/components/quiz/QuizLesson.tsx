// Deprecated legacy component for lesson-level quizzes. Quizzes are now course-level.
// Keeping a no-op export to avoid import errors if any stale references remain.
export default function QuizLesson() {
  return null;
}